// Final final
#include <iostream>
using namespace std;


/*
take two heeps, return 3rd larges number contained in both heaps
-maxheap, at least 3 elements 

bronze(heap1:Heap,Heap2:Heap):integer{
	count:int =0
	While (heap 1 and heap 2 are not Empty){
		
		tempHeap1:int peek()
		tempHeap2:int peek()

		if (TempHeap1 > temp heap2 ){
			remove tempHeap1
			increase count by 1
		}else{
			remove tempHeap2
			increase count by one
		}
		if (count == 3){
			if (heap1.peek()>heap2.peek())
			return heap1.peek()
			else 
			return heap2.peek()
		}

	}
}





*/




/*
return number of odd integers in node based binary search tree containng positive int 

int odds(BinaryNode* nodePtr){
	int count =0;

	if (nodePtr!=NULL){
		odds(nodePtr->getLeftChildPtr());
		if (nodePtr->getItem() %2 != 0){
		count+=1;
	}odds(nodePtr->getRightChildPtr())
	return count;
}






*/

int main() {





	return 0;
}